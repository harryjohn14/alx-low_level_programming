#include <stdio.h>

/**
* main - Print the numbers of base 16 in lowercase
*
* Return: Always 0 (Success)
*/
int main(void)
{
char y;

for (y = '0'; y <= '9'; ++y)
putchar(digit);
for (y = 'a'; y <= 'f'; ++y)
putchar(y);

putchar('\n');

return (0);
}
